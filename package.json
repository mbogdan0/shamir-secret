{
  "name": "shamir-secret",
  "version": "0.3.5",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite src",
    "build": "node --experimental-strip-types scripts/build.ts",
    "preview": "vite preview dist",
    "test": "node --experimental-strip-types --test \"test/*.test.ts\"",
    "test:coverage": "node --experimental-strip-types --experimental-test-coverage --test-coverage-lines=98 --test-coverage-branches=94 --test-coverage-functions=100 --test-coverage-include=\"src/ts/**/*.ts\" --test-coverage-include=\"scripts/*.ts\" --test-coverage-exclude=\"test/**\" --test-coverage-exclude=\"scripts/reference-interop.ts\" --test-coverage-exclude=\"scripts/verify-official-vectors*.ts\" --test \"test/*.test.ts\"",
    "lint": "npm run lint:ts && npm run lint:css && npm run format:check",
    "lint:ts": "eslint . --max-warnings=0",
    "lint:css": "stylelint \"src/**/*.css\" --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "audit": "npm audit --audit-level=moderate",
    "audit:prod": "npm audit --omit=dev --audit-level=moderate",
    "audit:signatures": "npm audit signatures",
    "sbom": "npm sbom --package-lock-only --sbom-format=cyclonedx",
    "verify:build": "node --experimental-strip-types scripts/build.ts --check",
    "verify:official-vectors": "node --experimental-strip-types scripts/verify-official-vectors.ts",
    "verify:official-vectors:upstream": "node --experimental-strip-types scripts/verify-official-vectors-upstream.ts",
    "verify:runtime-deps": "node --experimental-strip-types scripts/verify-runtime-deps.ts",
    "verify:source-policy": "node --experimental-strip-types scripts/verify-source-policy.ts",
    "test:reference-interop": "node --experimental-strip-types scripts/reference-interop.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run lint && npm run typecheck && npm run verify:build && npm run verify:official-vectors && npm run test:coverage && npm run verify:runtime-deps && npm run verify:source-policy && npm run build",
    "prepare": "husky"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.6.0",
    "eslint": "^10.3.0",
    "fast-check": "^4.7.0",
    "globals": "^17.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "playwright": "^1.59.1",
    "prettier": "^3.8.3",
    "stylelint": "^17.9.1",
    "stylelint-config-standard": "^40.0.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.1",
    "vite": "^8.0.10"
  },
  "lint-staged": {
    "*.{ts,tsx,js,mjs,cjs,css,html,json,md,yml,yaml}": [
      "prettier --write"
    ],
    "*.{ts,tsx,js,mjs,cjs}": [
      "eslint --max-warnings=0"
    ],
    "*.css": [
      "stylelint --max-warnings=0"
    ]
  }
}
