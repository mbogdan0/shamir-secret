name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements-dev.txt

      - name: Install dependencies
        run: npm ci

      - name: Install Python reference dependencies
        run: python -m pip install -r requirements-dev.txt

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Verify committed build artifact
        run: npm run verify:build

      - name: Verify vendored official vectors
        run: npm run verify:official-vectors

      - name: Test with coverage
        run: npm run test:coverage

      - name: Test Trezor reference interoperability
        env:
          PYTHON: python
        run: npm run test:reference-interop

      - name: Verify zero runtime dependencies
        run: npm run verify:runtime-deps

      - name: Verify runtime source policy
        run: npm run verify:source-policy

      - name: Audit dependencies
        run: npm run audit

      - name: Audit production dependency surface
        run: npm run audit:prod

      - name: Verify npm registry signatures
        run: npm run audit:signatures

      - name: Build
        run: npm run build

      - name: Generate CycloneDX SBOM
        run: npm run --silent sbom > sbom.cdx.json

      - name: Validate SBOM
        run: |
          node -e 'const fs = require("node:fs"); const sbom = JSON.parse(fs.readFileSync("sbom.cdx.json", "utf8")); if (sbom.bomFormat !== "CycloneDX" || !Array.isArray(sbom.components)) throw new Error("Invalid CycloneDX SBOM");'
